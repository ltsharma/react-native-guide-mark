# react-native-guide-mark
*Props*
title (String),
description (String),
onButtonPress (Fn),
onMarkPress (Fn),
buttonTitle (String),
visible (Boolean),
left (Number or String),
top (Number or String),
markSize (Number),
markImage (Object or require() )